#include<stdio.h>
#include<windows.h>
#include<unistd.h>


void ifte(int a,int b)
{
    COORD position;
    position.X = a;
    position.Y = b;
    SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE),position);
}

int main()

{


    int i,xa,ya;
    int time= 200000;

    while(1)
    {

    for(i=5;i<=30;i+=1)
    {
        int j = 15;

        ///loop for I:

        //* for I upper horizontal part:
        ifte(i,j);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for I vertical part:
        xa = i+2;
        ya = j+1;
        for(int k=0;k<3;k+=1)
        {
            ifte(xa,ya+k);
            printf("*");
        }
         

        //* for I lower horizontal part:
        ifte(i,j+4);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //space for I upper horizontal part(forward I):
        ifte(i-5,j);
        for(int k=1;k<=5;k+=1)
            printf(" ");
         

        //space for I  vertical part:
        xa = i + 1;
        ya = j + 1;
        for(int k=0;k<3;k+=1)
        {
            ifte(xa,ya+k);
            printf(" ");
        }
         

        //space for I lower horizontal part(forward I):
        ifte(i-5,j+4);
        for(int k=1;k<=5;k+=1)
            printf(" ");
         





        ///loop for F:
        //space for F vertical part
        xa = i + 5;
        for(int k=0;k<5;k+=1)
        {
            ifte(xa,j+k);
            printf(" ");
        }
         

        //* for F upper horizontal part
        ya = j;
        ifte(i+6,ya);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for F middle horizontal part
        ifte(i+6,j+2);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for F vertical part
        xa = i + 6;
        for(int k=0;k<5;k+=1)
        {
            ifte(xa,j+k);
            printf("*");
        }
         



        ///loop for T:
        //* for T upper horizontal part
        ifte(i+12,j);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for T vertical part
        xa = i + 14;
        for(int k=1;k<5;k+=1)
        {
            ifte(xa,j+k);
            printf("*");
        }
         

        //space for T upper horizontal part(forward T)
        ifte(i+11,j);
        printf(" ");
         

        //space for T vertical part
        xa = i + 13;
        for(int k=1;k<=5;k+=1)
        {
            ifte(xa,j+k);
            printf(" ");
        }
         




        ///loop for E:

        //* for E upper horizontal part
        ifte(i+18,j);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for E middle horizontal part
        ifte(i+18,j+2);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for E lower horizontal part
        ifte(i+18,j+4);
        for(int k=1;k<=5;k+=1)
            printf("*");
         

        //* for E vertical part
        xa = i + 18;
        for(int k=0;k<5;k+=1)
        {
            ifte(xa,j+k);
            printf("*");
        }
         

        //space for E vertical part
        xa = i + 17;
        for(int k=0;k<5;k+=1)
        {
            ifte(xa,j+k);
            printf(" ");
        }
         

        usleep(time);
    }
    printf("\033[2J");
}


    getch();
}